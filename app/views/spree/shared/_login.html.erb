<%= form_for Spree::User.new, as: :spree_user, url: spree.create_new_session_path do |f| %>
  <div id="password-credentials">
    <p>
      <!--<%= f.label :email, t('spree.email') %><br />-->
      <%= f.email_field :email, class: 'title', tabindex: 1, autofocus: true, placeholder: "Adresse e-mail" %>
    </p>
    <p>
      <!--<%= f.label :password, t('spree.password') %><br />-->
      <%= f.password_field :password, class: 'title', tabindex: 2, placeholder: "Mot de passe" %>
    </p>
  </div>
  <p>
    <%= f.check_box :remember_me, tabindex: 3 %>
    <%= f.label :remember_me, t('Se rappeler de moi') %>
  </p>

  <p>En vous connectant, vous accepter de vous conformer à notre Politique de confidentialité et à nos Conditions générales d'utilisation.</p>

  <p><%= f.submit t('spree.login'), class: 'button button-primary', tabindex: 4 %></p>
<% end %>
